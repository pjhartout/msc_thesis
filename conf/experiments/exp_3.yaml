description: "Look at the power of ESM to investigate ability to detect mutations"

compute:
  n_jobs: 32
  n_parallel_runs: 2
  
results: "data/experiments/esm_mutate/"

n_runs: 10

sample_size: 100


perturbations:
  mutation:
    min: 0
    max: 1
    step: 0.1
